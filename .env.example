# Copy to .env and fill in. .env is git-ignored.
#
# Only the API key is needed. If you were also issued a shared secret,
# discard it: this project calls no authenticated method, so the secret has
# no purpose here and can only ever be a liability.
LASTFM_API_KEY=
LASTFM_USER=ArjanKnol
