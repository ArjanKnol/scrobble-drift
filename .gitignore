# Never commit the API key.
.env
.env.*
!.env.example

.DS_Store
Thumbs.db
.idea/
.vscode/

# Local scratch. Nothing generated is committed: the analysis runs entirely in
# the visitor's browser and is stored only in their own IndexedDB.
*.log
scratch/
data/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
